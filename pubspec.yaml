name: test_solid
description: Test task for SOLID software
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  solid_lints: ^0.0.19

flutter:
  uses-material-design: true